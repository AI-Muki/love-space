import { Ionicons as OriginalIonicons } from '@expo/vector-icons';
import { LinearGradient } from 'expo-linear-gradient';
import { useRouter } from 'expo-router';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import { ScrollView, StyleSheet, Text, TouchableOpacity, View } from 'react-native';
import { useColors } from '@/hooks/useColors';
import { useSpace } from '@/context/SpaceContext';
const Ionicons = (props: any) => <OriginalIonicons {...props} name={props.name === 'arrow-up-right' ? 'arrow-forward-outline' : props.name} />;

export default function HomeScreen() {
  const colors = useColors(); const insets = useSafeAreaInsets(); const router = useRouter(); const { yourName, partnerName, togetherSince, activities, memories } = useSpace();
  const names = yourName && partnerName ? `${yourName} & ${partnerName}` : 'Your space together';
  const days = togetherSince ? Math.max(0, Math.floor((Date.now() - new Date(togetherSince).getTime()) / 86400000)) : 0;
  const next = activities.find((item) => item.status !== 'Done');
  return <ScrollView style={{ backgroundColor: colors.background }} contentContainerStyle={[styles.content, { paddingTop: insets.top + 18, paddingBottom: insets.bottom + 110 }]} showsVerticalScrollIndicator={false}><View style={styles.topline}><View><Text style={[styles.eyebrow, { color: colors.primary }]}>OUR SPACE</Text><Text style={[styles.title, { color: colors.foreground }]}>{names}</Text></View><TouchableOpacity onPress={() => router.push('/us')} style={[styles.avatar, { backgroundColor: colors.accent }]}><Ionicons name="people-outline" size={20} color={colors.accentForeground} /></TouchableOpacity></View><LinearGradient colors={[colors.primary, '#8D5369']} start={{ x: 0, y: 0 }} end={{ x: 1, y: 1 }} style={styles.hero}><View><Text style={styles.heroKicker}>YOUR LITTLE UNIVERSE</Text><Text style={styles.heroTitle}>Together for</Text><Text style={styles.days}>{days} <Text style={styles.daysLabel}>days</Text></Text><Text style={styles.heroFoot}>{togetherSince ? `Since ${togetherSince}` : 'Add the date you started'}</Text></View><View style={styles.orbit}><Ionicons name="sparkles" size={24} color="#F8E5DC" /></View></LinearGradient><Text style={[styles.sectionTitle, { color: colors.foreground }]}>A little look ahead</Text><TouchableOpacity onPress={() => router.push('/add')} style={[styles.nextCard, { backgroundColor: colors.card, borderColor: colors.border }]}><View style={[styles.iconBubble, { backgroundColor: colors.accent }]}><Ionicons name="compass-outline" size={24} color={colors.accentForeground} /></View><View style={{ flex: 1 }}><Text style={[styles.cardLabel, { color: colors.mutedForeground }]}>NEXT IDEA</Text><Text style={[styles.cardTitle, { color: colors.foreground }]}>{next?.title || 'Add your first idea'}</Text><Text style={[styles.cardMeta, { color: colors.mutedForeground }]}>{next ? next.category : 'What should you do together?'}</Text></View><Ionicons name="chevron-forward" size={20} color={colors.mutedForeground} /></TouchableOpacity><View style={styles.grid}><TouchableOpacity onPress={() => router.push('/score')} style={[styles.smallCard, { backgroundColor: colors.secondary }]}><Ionicons name="trophy-outline" size={20} color={colors.primary} /><Text style={[styles.cardLabel, { color: colors.mutedForeground }]}>US VS US</Text><Text style={[styles.smallTitle, { color: colors.foreground }]}>{useSpace().matches.length} matches</Text><Text style={[styles.cardMeta, { color: colors.mutedForeground }]}>Keep score together</Text></TouchableOpacity><TouchableOpacity onPress={() => router.push('/us')} style={[styles.smallCard, { backgroundColor: '#F1E4E7' }]}><Ionicons name="images-outline" size={20} color="#9B536B" /><Text style={[styles.cardLabel, { color: '#9B7180' }]}>MEMORIES</Text><Text style={[styles.smallTitle, { color: colors.foreground }]}>{memories.length} saved</Text><Text style={[styles.cardMeta, { color: '#9B7180' }]}>Your story, your way</Text></TouchableOpacity></View><TouchableOpacity onPress={() => router.push('/add')} style={[styles.pickButton, { backgroundColor: colors.foreground }]}><Ionicons name="add-circle-outline" size={22} color={colors.background} /><Text style={[styles.pickText, { color: colors.background }]}>ADD SOMETHING TO OUR SPACE</Text><Ionicons name="arrow-up-right" size={19} color={colors.background} /></TouchableOpacity></ScrollView>;
}
const styles = StyleSheet.create({ content: { paddingHorizontal: 20, gap: 14 }, topline: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', marginBottom: 8 }, eyebrow: { fontSize: 11, letterSpacing: 2.2, fontWeight: '700', marginBottom: 5 }, title: { fontSize: 25, fontWeight: '700', letterSpacing: -0.6 }, avatar: { width: 42, height: 42, borderRadius: 21, alignItems: 'center', justifyContent: 'center' }, hero: { borderRadius: 26, padding: 22, minHeight: 174, overflow: 'hidden', flexDirection: 'row', justifyContent: 'space-between' }, heroKicker: { color: '#F5D9D0', fontSize: 10, letterSpacing: 1.8, fontWeight: '700', marginBottom: 11 }, heroTitle: { color: '#FBEDE7', fontSize: 16 }, days: { color: '#FFF9F5', fontSize: 52, lineHeight: 59, fontWeight: '700', letterSpacing: -2 }, daysLabel: { fontSize: 18, letterSpacing: 0, fontWeight: '500' }, heroFoot: { color: '#F3D8D0', fontSize: 11, marginTop: 7 }, orbit: { width: 70, height: 70, borderRadius: 35, backgroundColor: 'rgba(255,255,255,0.12)', alignItems: 'center', justifyContent: 'center', marginTop: 20 }, sectionTitle: { fontSize: 18, fontWeight: '700', marginTop: 13 }, nextCard: { flexDirection: 'row', alignItems: 'center', gap: 13, padding: 15, borderRadius: 20, borderWidth: 1 }, iconBubble: { width: 48, height: 48, borderRadius: 16, alignItems: 'center', justifyContent: 'center' }, cardLabel: { fontSize: 10, letterSpacing: 1.1, fontWeight: '700', marginBottom: 4 }, cardTitle: { fontSize: 17, fontWeight: '700' }, cardMeta: { fontSize: 12, marginTop: 3 }, grid: { flexDirection: 'row', gap: 12 }, smallCard: { flex: 1, borderRadius: 21, padding: 15, minHeight: 132, gap: 5 }, smallTitle: { fontSize: 16, fontWeight: '700', marginTop: 2 }, pickButton: { height: 56, borderRadius: 18, flexDirection: 'row', alignItems: 'center', paddingHorizontal: 17, gap: 10, marginTop: 4 }, pickText: { flex: 1, fontSize: 12, letterSpacing: 1.1, fontWeight: '700' } });